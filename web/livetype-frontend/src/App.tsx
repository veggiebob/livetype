import { useState } from 'react'
import './App.css'

/* message interface */

function App({username, wsRef}: {username: string, wsRef: any}) {
  const ws = wsRef.current;

  // show each message in its own div
  return <></>
}

export default App
